#!/bin/bash

set -e

simdir="/scratch/le8016/Cactus_HDF5_combiner_run"
submitscript="SPORC_NVIDIA.sub"
submit_cmd="sbatch"

mkdir ${simdir}
cp ../Cactus_HDF5_combiner_exe ${simdir}
cp ../SubmitScripts/${submitscript} ${simdir}
cp ../Parameters.hh ${simdir}
cd ${simdir}
${submit_cmd} ${submitscript}

echo ""
echo "Job submitted successfully"
echo ""
